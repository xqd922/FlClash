name: proxy
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  # NOTE: 对齐主 app 的 SDK 约束 (>=3.8.0)，避免声称兼容但未测试的版本。
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter:
  plugin:
    platforms:
      windows:
        pluginClass: ProxyPluginCApi
